<!-- Excercise 0.6 New note in Single page app diagram -->

```mermaid
sequenceDiagram
    participant browser
    participant server

Note right of browser: User writes a new note and clicks the Save-button on the form

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa JSON data { "content": "Example", "date": "2026-1-14" }
    activate server
Note right of browser: Browser sends JSON data, and server reponds JSON data status code 201
    server-->>browser: JSON data {"message":"note created"}
    deactivate server

    Note right of browser: The browser executes the JavaScript code that renders the notes
```